## Використання gTTS - Phyton Python 3.14.5
```
python --version
py -m venv .venv

.venv\Scripts\activate.bat
source .venv/bin/activate

python.exe -m pip install --upgrade pip
python3 -m pip install --upgrade pip

pip install jupyter

pip list

jupyter notebook
```